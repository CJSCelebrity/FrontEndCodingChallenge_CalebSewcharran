# CalebSewcharran_CodingChallengeFrontEnd

## Build Setup

```bash
# To install the dependencies needed for the project, run the command below
$ npm install

# To serve the project in your browser, run the command below
$ npm run dev

```
